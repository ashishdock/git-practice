// Demonstrate ArrayDeque.
import java.util.*;

class ArrayDequeDemo
{
	public static void main(String args[])
	{
		// Create and array deque.
		ArrayDeque<String> adq = ne wArrayDeque<String>();
		
		// Use an ArrayDeque like a stack
		adq.push("A");
		adq.push("B");
		adq.push("D");
		adq.push("E");
		adq.push("F");
		
		System.out.println("Popping the stack.");
		
		while(adq.peek() != null)
			System.out.print(adq.pop() + " ");
		
		System.out.println();
	}
}